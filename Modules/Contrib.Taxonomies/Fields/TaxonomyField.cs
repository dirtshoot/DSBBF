using Orchard.ContentManagement;

namespace Contrib.Taxonomies.Fields {
    /// <summary>
    /// This field has not state, as all terms are saved in specific tables
    /// </summary>
    public class TaxonomyField : ContentField {
    }
}